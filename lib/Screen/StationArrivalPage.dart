import 'package:flutter/material.dart';

class StationArrivalPage extends StatelessWidget {
  const StationArrivalPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

