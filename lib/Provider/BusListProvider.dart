import 'package:flutter/material.dart';

class BusListProvider extends StatefulWidget {
  const BusListProvider({Key? key}) : super(key: key);

  @override
  State<BusListProvider> createState() => _BusListProviderState();
}

class _BusListProviderState extends State<BusListProvider> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
